spring:
  application:
        name: subscription
  datasource:
    url: jdbc:postgresql://localhost:5432/subscriptiondb
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    "[org.springframework.boot.actuate.info]": DEBUG

info:
  app:
    name: Subscription Service
    description: AI-powered subscription risk analysis platform
    version: 0.0.1
  
management:
  info:
    env:
      enabled: true
  endpoints:
    web:
      exposure:
        include: health,info,metrics,env
  endpoint:
    health:
      show-details: always
    
